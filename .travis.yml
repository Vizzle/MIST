language: objective-c
cache: cocoapods
xcode_workspace: MIST.xcworkspace
xcode_scheme: MIST
osx_image: xcode9.4
xcode_sdk: iphonesimulator
branches:
  only:
  - master
before_install:
  - pod update
  
